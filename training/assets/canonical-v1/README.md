# Canonical training assets

This folder is reserved for the approved exercise-image catalog and its style references. The web app must use only the canonical catalog defined here; legacy SVG/sprite assets are not valid fallbacks.
